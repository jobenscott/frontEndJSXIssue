import React from 'react';


export default function CurrentCards({cards}) {
//   const classes = useStyles();
//   const bull = <span className={classes.bullet}>•</span>;
  
  return (
    // <div className="card-container">
        cards.map((value, index) => value)
    // </div>
  );
}
